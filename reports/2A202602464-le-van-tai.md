# Báo cáo đóng góp cá nhân

## Thông tin

- Họ và tên: Lê Văn Tài
- Mã học viên: 2A202602464
- Nhóm: 22
- Repository: `taitottinhday/K4-L3A-RAG-Pipeline`
- Nhánh: `feature/generation-evaluation`

## Phần việc đã thực hiện

| Module/deliverable | Việc trực tiếp thực hiện | File/commit | Trạng thái |
| --- | --- | --- | --- |
| Generation | Hoàn thiện reorder, format context, provider dispatch, citation và safe refusal | `src/task10_generation.py`, `5779b21` | Done |
| Streamlit UI | Nối pipeline end-to-end, hiển thị answer, nguồn, retrieval method và score | `app.py`, `5779b21` | Done |
| Evaluation | Xây dựng A/B dense-only và hybrid + RRF, bốn proxy metric, threshold calibration và failure analysis | `src/evaluate_pipeline.py`, `group_project/evaluation/`, `5779b21` | Done |
| Integration | Cập nhật cấu hình, README, TEAMMATES và chạy kiểm tra tích hợp | `pyproject.toml`, `README.md`, `TEAMMATES.md`, `5779b21` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Dùng Gemini làm provider generation mặc định và hash embedding local cho retrieval mặc định.  
   **Lý do/evidence:** Gemini key đã hoạt động; hash embedding giúp index/query tái lập mà không phụ thuộc quota embedding.  
   **Trade-off:** Chất lượng semantic thấp hơn embedding model chuyên dụng, nhưng BM25 và RRF bù cho số hiệu văn bản/từ khóa chính xác.

2. **Quyết định:** Fallback dựa trên cosine dense gốc với threshold `0.30`, không dùng điểm RRF.  
   **Lý do/evidence:** Query trong domain có best dense cosine khoảng `0.345`; query World Cup ngoài domain khoảng `0.155`.  
   **Trade-off:** PageIndex là tùy chọn; khi provider trống/lỗi, pipeline giữ hybrid result và generation từ chối nếu không đủ bằng chứng.

## Kiểm thử và kết quả

- Contract tests: `15 passed`.
- Acceptance tests: `5 passed`.
- Toàn bộ test: `20 passed`.
- Query trong domain trả đúng ngày hiệu lực `26-06-2025` kèm citation `[1], [2]`.
- Query ngoài domain trả safe refusal, không bịa thông tin.
- Config A dense-only đạt average `0.870`; Config B hybrid + RRF đạt `0.893`, tăng `0.023`.

## Điều còn hạn chế

- Proxy metric local không thay thế đánh giá RAGAS hoặc LLM judge.
- Hash embedding ưu tiên tính tái lập hơn semantic quality.
- Nếu có thêm thời gian, bước đầu tiên là đánh giá thêm embedding model semantic trên cùng golden dataset và đo latency/cost.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh phần việc tôi phụ trách và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/09/2026
- Tên thành viên: Lê Văn Tài
